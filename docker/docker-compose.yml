name: timeline-maker
services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5051:5051"
    volumes:
      - ../data:/data
    environment:
      - TIMELINE_DB=/data/timelines.db
      - FLASK_DEBUG=0
      - PORT=5051
    restart: unless-stopped
